
#include <stdio.h>

// QUEUES - ARRAY BASED IMPLEMENTATION

#define MAX_SIZE 10

int A[MAX_SIZE];
int front = -1, rear = -1;

bool isEmpty()
{
    if(front = -1 && rear = -1)
        return true;
    else
        return false;
}

